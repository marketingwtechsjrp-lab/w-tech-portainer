version: '3.8'

services:
  # Python Backend
  meta-api:
    image: wtech-meta-api:latest
    restart: unless-stopped
    networks:
      - hidro
    deploy:
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.wtech-api.rule=Host(`site2.w-techbrasil.com.br`) && PathPrefix(`/api`)"
        - "traefik.http.routers.wtech-api.entrypoints=websecure"
        - "traefik.http.routers.wtech-api.tls.certresolver=letsencryptresolver"
        - "traefik.http.services.wtech-api.loadbalancer.server.port=5000"
        - "traefik.http.routers.wtech-api.priority=2"

  # React Frontend
  frontend:
    image: wtech-frontend:latest
    restart: unless-stopped
    networks:
      - hidro
    deploy:
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.nh-frontend.rule=Host(`site2.w-techbrasil.com.br`)"
        - "traefik.http.routers.nh-frontend.entrypoints=websecure"
        - "traefik.http.routers.nh-frontend.tls.certresolver=letsencryptresolver"
        - "traefik.http.services.nh-frontend.loadbalancer.server.port=80"
        - "traefik.http.routers.nh-frontend.priority=1"

networks:
  hidro:
    external: true
